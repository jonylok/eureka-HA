spring:
  application:
    name: eureka

server:
  port: 8763

eureka:
  instance:
    hostname: eureka3
    appname: eureka
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/